# 릴리스 노트

자세한 사용자 영향과 사용 시 유의 사항은 [사용자 가이드의 릴리스 노트](docs/gitbook/release-notes.md)를 참고하세요.

## v2.0.0

- **로컬 역공학 기반 최종뎀·환산·헥사환산 실시간 계산 엔진 (`fd_calculator.js`) 탑재**:
  - 외부 환산 API 호출 없이(정책 위반 0%) 브라우저 로컬 스토리지의 `character-store` 데이터를 활용하여 경매장 찜 목록 장비의 장착 시 최종 데미지 변화량(`eff`), 일반 환산(`hwanDiff`), 헥사 환산(`hexaDiff`)을 실시간 계산 및 배지 표시
  - 환산 사이트 웹팩 번들(`chunk 3723`) 역공학을 통한 전체 방어구·장신구·칠흑·여명·도전자·에테르넬 세트 효과(`SET_OPTION_TABLE`) 1:1 정밀 반영
  - 해방된 `제네시스`/`데스티니` 무기의 에테르넬 세트 `+1` 및 하위 방어구 세트(3세트 이상) 럭키 아이템 `+1` 특수 시너지 완벽 구현
  - 3차 에르미트 스플라인(`Hermite Cubic Spline`) 역함수 및 주스탯·스탯% 이차 교차항 보정으로 실측 오차 `±0.02%p` 이내 달성
- **다중 캐릭터 프로필 로컬 캐싱(`characterStoreCache`) 및 기준 캐릭터 전환 (`#calcCharSelect`)**:
  - 환산 사이트에서 조회한 본캐·부캐 스펙 데이터를 자동 캐싱하여, 팝업 상단 드롭다운에서 언제든 기준 캐릭터를 바꾸고 해당 캐릭터 기준의 최종뎀·환산 변화를 즉시 비교 가능
- **경매장 찜 목록 원클릭 정렬 필터 (`#auctionSortSelect`)**:
  - `찜한 순서`, `최종뎀 높은 순`, `가성비(10억당 FD) 순`, `직작 대비 꿀매 순`, `가격 낮은 순` 5가지 정렬 모드 지원
- **직작 원가 대비 경매장 매물 손익분기 판정 배지 (`evaluateCraftVsBuy`)**:
  - 스타포스/잠재/에디 기댓값 및 가횟(가위 사용 가능 횟수) 감가를 반영하여 추정 직작 원가 대비 현재 경매장 가격을 `🟢 꿀매`, `🟡 적정가`, `🔴 직작 추천` 배지로 표시
- **스펙업 순서 [추가스펙(`bookMarkSimulList`)] 원클릭 연동 (`#exportSimulBtn`)**:
  - 경매장 찜 목록에서 선택한 매물들을 원하는 캐릭터 이름을 입력받아 해당 캐릭터의 `스펙업 순서` 페이지(`https://maplescouter.com/ko/spec-order?name=...`)로 이동 후 `bookMarkSimulList`에 자동 등록
- **신규 소울 잠재능력 패치 대응 (`mapper.js`)**:
  - 아이템메이커 변환 객체에 신규 소울 스키마 필드(`soul_potential_option_1`, `soul_atk`, `soul_potential_grade`, `soul_potential_amplified_grade`)를 기본 포함하여 환산 사이트에서 '소울 이전 장비 복구' 팝업이 뜨는 현상 해결

## v1.1.2

- 마지막 스펙업 분석 결과와 분석 시각을 저장해 팝업을 다시 열어도 확인할 수 있도록 개선
- 스펙업 순서 페이지가 아닌 곳에서 분석을 실행하지 않도록 페이지 확인 추가
- 교환가능 장비에는 가중치를 적용하지 않고, 교환불가 항목에만 교환불가 가중치를 적용하도록 수정
- 추가옵션 비용을 검증 전까지 `검증 중`으로 표시하고 총 필요 기댓값 계산에서 제외

## v1.1.1

- 교환불가 장비·심볼·북마크에 교환불가 가중치 반영
- 현재 조회 캐릭터와 일치하는 북마크만 분석에 포함
- 비용이 0인 심볼 항목이 분석에서 누락되지 않도록 보완

## v1.0.0 (Alpha)

첫 번째 메이저 릴리즈입니다. 메이플스토리 장비 데이터의 추출부터 환산 사이트 주입까지의 전체 파이프라인이 안정화되었습니다.

## 주요 변경 사항 (Key Updates)
- **v1.0.0 골자 완성:** 장비 데이터 28개 표준 스키마 및 19개 스탯 키 무결성 보장.
- **폴더화 시스템:** `chrome.storage.local` 기반 다중 캐릭터 관리 폴더 시스템 구현.
- **안정성 강화:** SPA 환경에서의 리로드 예외 처리 및 비동기 주입 아키텍처 재설계.
- **테스트 환경 구축:** `Jest` 기반 단위 테스트 환경 구축 완료 (도메인 로직 검증).

## 개선 사항
- 가져온 장비 목록에서의 전체 선택/해제 기능 추가.
- 커스텀 장비(My) 불러오기 및 슬롯 화이트리스트 필터링 도입.
- UX 개선: 비즈니스 플로우 단계별 컨펌/안내 알림창 표준화.

## 개발자 노트
이 버전은 단순한 데이터 변환을 넘어, 익스텐션의 확장성과 유지보수를 고려한 첫 번째 메이저 버전입니다. 특히 `mapper.js`의 단위 테스트 구축을 통해 향후 어떤 변경에도 데이터 정합성을 보장할 수 있는 환경을 마련했습니다.

## 라이센스
본 프로젝트는 MIT 라이센스를 따릅니다.
